# Copy to `.env.development` and adjust locally (not committed with secrets).
VITE_API_PROXY_TARGET=http://localhost:8080

# Optional: override TripleDES defaults from `src/utils/utils.ts`
# VITE_TRIPLEDES_KEY=
# VITE_TRIPLEDES_IV=
